'use strict';

describe('muslink.groups module', function() {

  beforeEach(module('muslink.groups'));

  describe('groups controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var groupsCtrl = $controller('BandsCtrl');
      expect(groupsCtrl).toBeDefined();
    }));

  });
});